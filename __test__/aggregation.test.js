import { aggregateSampleOtusBySite } from "../src/aggregation";

// require aggregateSampleOtusBySite

test("Aggregating Sample Otu entries into a summed values", () => {
  //   let hi = [1, 2];
  aggregateSampleOtusBySite(null);
});
